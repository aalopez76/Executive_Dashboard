# Dependencias DIRECTAS del dashboard (fuente). El lockfile requirements.txt se genera con:
#   pip-compile requirements.in -o requirements.txt
# Stack BI: Vizro/Dash + pandas + gunicorn. El conector SQL se instala editable desde el submódulo.

vizro==0.1.44
pandas==2.2.2
gunicorn==23.0.0
-e ./SQL-Connection-Module
